spring:
  application:
    name: ecommerce-api

  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: test
    password: test

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

jwt:
  secret: test